# Implementation Plan: Complete Red Theme Conversion

**Branch**: `002-complete-theme-fix` | **Date**: 2025-01-11 | **Spec**: [spec.md](./spec.md)
**Input**: Feature specification from `/specs/002-complete-theme-fix/spec.md`

**Note**: This template is filled in by the `/speckit.plan` command. See `.specify/templates/commands/plan.md` for the execution workflow.

## Summary

完成 NOFX Web Dashboard 的红色主题转换，修复所有剩余的黄色UI元素和灰色文本可读性问题。主要工作包括：
1. 将所有黄色颜色值（`#F0B90B`、`var(--brand-yellow)`、`var(--binance-yellow)`）替换为红色（`var(--brand-red)`）
2. 将灰色文本（`#848E9C`、`#EAECEF`）替换为深色文本（`var(--text-primary)`）以提高可读性
3. 确保所有背景使用浅色主题变量

技术方法：使用 `edit` 和 `multi_edit` 工具批量替换颜色值，优先处理用户最常访问的页面（/traders, /competition），然后处理配置界面和FAQ页面。

## Technical Context

**Language/Version**: TypeScript 5.8 + React 18.3  
**Primary Dependencies**: Vite 6.0, Tailwind CSS 3.4, Framer Motion 11.15, Lucide React 0.468  
**Storage**: N/A (纯前端样式修改)  
**Testing**: 手动测试 + Playwright MCP 浏览器验证  
**Target Platform**: Web (现代浏览器，支持 ES2020+)
**Project Type**: Web application (单页应用 SPA)  
**Performance Goals**: 无性能影响（仅CSS变量和颜色值替换）  
**Constraints**: 
- 必须保持 WCAG 2.1 AA 对比度标准（文本对比度 ≥ 4.5:1）
- 不能破坏现有功能
- 不能引入新的依赖
**Scale/Scope**: 
- 19个文件需要修改
- 103处黄色颜色值
- 232处灰色文本颜色
- 预计影响 15+ 个页面/组件

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

Reference: `.specify/memory/constitution.md`

### I. Component Reuse First
- [x] Verified existing components in `src/components/`, `src/hooks/`, `src/lib/`, `src/utils/`
- [x] Reviewed `package.json` dependencies for available libraries
- [x] Documented search results and justification for any new components
- [x] **GATE**: If creating new components/utils, user confirmation obtained

**Status**: ✅ PASS - 不需要创建新组件，仅修改现有组件的样式属性（颜色值）

### II. Documentation Language Protocol
- [x] Document structure (headings, keywords) uses English
- [x] Human-readable content written in 中文 (Chinese)
- [x] Agent-readable content (code, APIs, paths) uses English
- [x] Code comments: 中文 for business logic, English for technical details

**Status**: ✅ PASS - 规范文档遵循双语协议

### III. Type Safety & Linting
- [x] All types defined in `src/types/` or inline
- [x] No `any` types without justification
- [x] ESLint configuration followed
- [x] Prettier formatting applied

**Status**: ✅ PASS - 仅修改样式属性，不涉及类型变更。修改后将运行 `npm run lint:fix`

### IV. UI/UX Standards
- [x] Uses existing UI stack: Radix UI, Tailwind CSS, Lucide React, Framer Motion
- [x] Responsive design (mobile-first)
- [x] Accessibility: keyboard nav, ARIA labels, screen reader support
- [x] Loading states and error boundaries planned

**Status**: ✅ PASS - 保持现有UI栈，改进可访问性（提高文本对比度符合WCAG 2.1 AA标准）

### V. State Management & Data Fetching
- [x] State approach: useState/useReducer (local), Zustand (global), SWR (server)
- [x] API calls use `httpClient` from `src/lib/httpClient.ts`
- [x] Endpoints centralized in `src/lib/api.ts`
- [x] Types defined for all API responses

**Status**: ✅ PASS - 不涉及状态管理或数据获取的修改

## Project Structure

### Documentation (this feature)

```text
specs/002-complete-theme-fix/
├── spec.md              # Feature specification (completed)
├── plan.md              # This file (implementation plan)
├── research.md          # N/A (no research needed for style changes)
├── data-model.md        # N/A (no data model changes)
├── quickstart.md        # N/A (no new features to document)
├── contracts/           # N/A (no API changes)
└── tasks.md             # To be generated by /speckit.tasks command
```

### Source Code (repository root)

```text
src/
├── components/
│   ├── AITradersPage.tsx          # 🔴 HIGH PRIORITY (25 yellow + 83 gray)
│   ├── TraderConfigModal.tsx      # 🔴 HIGH PRIORITY (22 yellow + 41 gray)
│   ├── CompetitionPage.tsx        # 🟡 MEDIUM (7 yellow + 21 gray)
│   ├── AILearning.tsx             # 🟡 MEDIUM (9 yellow + 4 gray)
│   ├── EquityChart.tsx            # 🟡 MEDIUM (5 yellow + 22 gray)
│   ├── ComparisonChart.tsx        # 🟢 LOW (1 yellow + 15 gray)
│   ├── TraderConfigViewModal.tsx  # 🟢 LOW (3 yellow + 13 gray)
│   ├── ResetPasswordPage.tsx      # 🟢 LOW (3 yellow + 11 gray)
│   ├── Header.tsx                 # 🟢 LOW (2 yellow + 4 gray)
│   ├── faq/
│   │   ├── FAQContent.tsx         # 🟡 MEDIUM (17 yellow + 2 gray)
│   │   ├── FAQSidebar.tsx         # 🟢 LOW (4 yellow + 3 gray)
│   │   ├── FAQLayout.tsx          # 🟢 LOW (3 yellow + 6 gray)
│   │   └── FAQSearchBar.tsx       # 🟢 LOW (1 yellow + 3 gray)
│   └── ui/
│       └── input.tsx              # ✅ COMPLETED (already fixed)
├── pages/
│   └── FAQPage.tsx                # 🟢 LOW (0 yellow + 1 gray)
└── lib/
    └── httpClient.ts              # 🟢 LOW (1 yellow + 0 gray)
```

**Structure Decision**: Web application (单页应用)。所有修改都在 `src/components/` 目录下的现有文件中进行，不需要创建新文件或修改项目结构。

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

**Status**: ✅ 无违规项 - 所有 Constitution Check 项目均已通过，无需复杂性追踪。
